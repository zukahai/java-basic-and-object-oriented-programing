#include<bits/stdc++.h>
#include "../lib/library.h"
using namespace std;

int main() {
    vector<string> a(10);
    for (int i = 0; i < 10; i++) {
        a[i] = random_string(3);
    }
    // print_vector(a, cout);
}